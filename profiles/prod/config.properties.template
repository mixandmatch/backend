#This is the template for the maven profile specific database configuration
#To use the stage-dev configuration:
#1. Copy this file and rename it to config.properties
#2. Fill in the database connection url and credentials
#The file config.properties should not be added to the SCM!
jdbc.url=jdbc:mysql://localhost:3306/database
jdbc.username=root
jdbc.password=password
